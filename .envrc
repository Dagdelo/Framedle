# Framedle — direnv environment
# Activated automatically when you cd into this directory

# Git identity (personal — Dagdelo)
export GIT_AUTHOR_NAME="Dagdelo"
export GIT_AUTHOR_EMAIL="henrique@hd5.dev"
export GIT_COMMITTER_NAME="Dagdelo"
export GIT_COMMITTER_EMAIL="henrique@hd5.dev"

# SSH key for git operations (personal key)
export GIT_SSH_COMMAND="ssh -i ~/.ssh/henrique@hd5.dev_id_ed25519 -o IdentitiesOnly=yes"

# Node.js
export NODE_ENV="development"

# Source local secrets (if exists)
# Create .envrc.local from .envrc.local.example and fill in your secrets
if [ -f "$PWD/.envrc.local" ]; then
  source "$PWD/.envrc.local"
fi

echo "✓ Framedle environment active (Dagdelo)"
